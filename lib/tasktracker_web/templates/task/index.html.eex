<table class="table">
  <thead>
    <tr>
      <th>Status</th>
      <th>Title</th>
      <th>Owner</th>
      <th>Assignee</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for task <- @tasks do %>
    <tr class="<%= @colorClass%>">
      <td><%= task.status.statustype %></td>
      <td><%= task.title %></td>
      <td><%= task.owner.username %></td>
      <td>
        <%= if task.assignee do %>
          <%= task.assignee.username %>
        <% else %>
          <p> unassigned </p>
        <% end %>
      </td>

      <td class="text-right">
        <span><%= link "Show", to: task_path(@conn, :show, task), class: "btn btn-default btn-xs text-white" %></span>
        <span><%= link "Edit", to: task_path(@conn, :edit, task), class: "btn btn-default btn-xs text-white" %></span>
        <span><%= link "Delete", to: task_path(@conn, :delete, task), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs text-white" %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

